<section class="hero is-small home-bkg-img is-bold">
    <div class="hero-body">
        <div class="container has-text-centered">
            <div class="box is-vcentered has-background-grey-lighter quizBlock">
                <div class="content">
                    <div class="box">

                        {{!-- Question Block, this is where the questions will be rendered --}}
                        <div id="questionBlock">
                            <h1 class="title" data-quizid="{{quizID}}" id="questionText">{{quiz}}</h1>
                            <p id="quizDescription">Welcome to the <strong>"{{quiz}}"</strong> quiz! Click "start"
                                to begin your
                                quiz.</p>
                        </div>
                    </div>

                    {{!-- Answer Block, this is where the answer choices are rendered --}}
                    <div id="answerBlock"></div>

                    {{!-- Result Block, this is where the results will be rendered after quiz completion --}}
                    <div id="resultBlock"></div>

                    {{!-- Start Button, rendered on initial screen, hidden after click --}}
                    <button class="button is-personify-purple" id="nextButton">START</button>
                </div>
            </div>
        </div>
    </div>
</section>

<div class="modal" id="about">
  <div class="modal-background about"></div>
  <div class="modal-card">
    <header class="modal-card-head">
      <p class="modal-card-title has-text-centered">How to use Personify</p>
      <button class="delete about" aria-label="close"></button>
    </header>
    <section class="modal-card-body">
      <p class="is-size-5">Personify is unique: it gives personality information in two different ways. First, the result of whatever personality tests you take. Second, our copyright personality composite. The personality composite is effected by every answer you have ever given, and gives the user of a breakdown of how often they answer in particular ways. <br> <br> Users must be logged in to see that composite on their profile or have their composite updated after a test!</p>
    </section>
  </div>
</div>

<script src="/assets/js/quiz.js"></script>